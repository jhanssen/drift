// The graph canvas element — a leaf module (no imports) so every module
// in the cyclic graph-view import web can hold it from evaluation time.
export const graphCanvas = document.getElementById('graphCanvas');
